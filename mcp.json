{
  "mcpServers": {
    "axonflow": {
      "type": "http",
      "url": "${AXONFLOW_ENDPOINT:-http://localhost:8080}/api/v1/mcp-server",
      "headersHelper": "bash -c 'if [ -n \"$AXONFLOW_AUTH\" ]; then echo \"{\\\"Authorization\\\": \\\"Basic $AXONFLOW_AUTH\\\"}\"; else echo \"{}\"; fi'"
    }
  }
}
